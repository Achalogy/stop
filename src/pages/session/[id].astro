---
import Base from "../../layouts/Base.astro";
import GameComponent from "../../components/GameComponent";
const { id } = Astro.params;
---

<Base>
  <div class="flex flex-col gap-2 w-full h-full p-8">
    <div class="flex gap-2">
      <div class="rounded bg-slate-50 p-6 w-full">
        <p><b>Sesión:</b> {id}</p>
      </div>
      <div class="rounded bg-slate-50 p-6 w-full">
        <b>Jugadores:</b>
      </div>
    </div>

    <GameComponent client:load id={id} />
  </div>
</Base>
